# Базовий образ
FROM node:18

# Встановлюємо робочу директорію в контейнері
WORKDIR /usr/src

# Копіюємо package.json і встановлюємо залежності
COPY package*.json ./
RUN npm install

# Копіюємо весь код додатка
COPY . .

# Експортуємо порт (залежно від вашого додатка)
EXPOSE 3000

# Вказуємо команду для запуску додатка
CMD ["npm", "start"]
